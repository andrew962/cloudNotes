import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.afam.cloudNotes',
  appName: 'NoteCloud',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
